name: daily-hotspot-update

on:
  schedule:
    # 北京时间 10:00 = UTC 02:00
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: hotspot-daily
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      ARK_API_KEY: ${{ secrets.ARK_API_KEY }}
      ARK_MODEL: ${{ secrets.ARK_MODEL }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 检查 API Key（未配置则跳过本次，等待配置）
        if: env.ARK_API_KEY == ''
        run: echo "ARK_API_KEY 尚未配置，本次更新跳过（不会报错）。请在仓库 Settings->Secrets 添加。" && exit 0

      - name: 抓取三平台热榜
        run: python scripts/fetch_hot.py

      - name: 生成双品牌借势建议（方舟 LLM）
        run: python scripts/llm_advise.py

      - name: 构建双品牌 HTML
        run: python scripts/build.py

      - name: 保存历史数据快照（用于热点生命周期追踪）
        run: |
          mkdir -p history
          cp hotdata.json history/$(date +%F).json
          echo "已保存历史快照: history/$(date +%F).json"

      - name: 提交并推送
        run: |
          git config user.name "hotspot-bot"
          git config user.email "hotspot-bot@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "无内容变化，跳过提交"
          else
            git commit -m "daily update: $(date +%F)"
            git push
            echo "已推送每日更新"
          fi
